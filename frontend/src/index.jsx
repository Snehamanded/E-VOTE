/************************** execution starts for this file ****************************/

import { ReactDOM, BrowserRouter, Router } from "./common/Imports";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <BrowserRouter>
    <Router />
  </BrowserRouter>
);
